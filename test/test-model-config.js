var Config = require('stitch/model/config').Model,
    Test,
    instance;

console.dir(Config);
// console.dir(Model);

instance = new Config();

// console.log(JSON.stringify(Test, null, 4));
// instance.destroy();
console.dir(instance);
